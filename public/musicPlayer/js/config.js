require.config({
    baseUrl : '/musicPlayer/js/',
    paths : {
        jquery : 'public/jquery-3.2.1',
        template : 'myplugins/template',
        dragdrop : 'myplugins/dragdrop',
        scrollbar : 'myplugins/scrollbar'
    }
});